# ── Clé secrète JWT (générer avec : python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=remplace_par_une_cle_secrete_longue_et_aleatoire

# ── Base de données relationnelle (SQLite par défaut, PostgreSQL en prod)
DATABASE_URL=sqlite:///./chatbot.db
# DATABASE_URL=postgresql://user:password@localhost:5432/chatbot_db

# ── API Mistral AI (https://console.mistral.ai)
MISTRAL_API_KEY=ta_cle_mistral_api

# ── MongoDB (local par défaut)
MONGO_URL=mongodb://localhost:27017
MONGO_DB=chatbot_universitaire
